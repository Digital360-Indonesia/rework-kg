---
import BaseLayout from '../layouts/BaseLayout.astro';

const metaTitle = 'Business — Seragam & Apparel Kerja | Kustom Garment';
const metaDescription = 'Seragam kantor, polo, kemeja, sampai wearpack — dibikin rapi dan on-brand untuk timmu. Tanpa minimum order, bordir logo beres, invoice & faktur pajak siap.';

// --- CMS-ready content ---
const hero = {
  // NOTE: placeholder wide banner — swap for a real landscape team/office photo when available.
  banner: '/images/placeholder/theme-image-1777040341996.png',
  bannerAlt: 'Tim kerja pakai seragam custom Kustom Garment',
  title: 'Seragam & apparel kerja',
  sub: 'Dari polo kantor sampai wearpack lapangan — bikin tim kamu tampil seragam, on-brand, dan nyaman dipakai kerja seharian.',
};

// Customer/client logos for the trust strip.
const clients = ['klien-1.png','klien-2.png','klien-3.png','klien-4.png','klien-5.png','klien-6.png','klien-7.png','klien-8.png','klien-9.png','klien-10.png','klien-11.png','klien-12.png','klien-13.png'];

// Peek of the categories (Bisnis is the current page).
const categoryPeek = [
  { name: 'Bisnis', desc: 'Seragam & apparel kerja', href: '/business/', image: '/images/categories/business.webp', current: true },
  { name: 'Komunitas', desc: 'Merch & seragam komunitas', href: '/community/', image: '/images/categories/community.webp' },
  { name: 'Kampus', desc: 'Jaket angkatan & apparel kampus', href: '/campus/', image: '/images/categories/campus.webp' },
  { name: 'Personal', desc: 'Custom satuan buat kamu', href: '/personal/', image: '/images/categories/personal.webp' },
];

const products = [
  { image: '/images/placeholder/sample-section3a4%201.png', title: 'Polo Kantor', desc: 'Pique cotton & cooldry, bordir logo dan nama divisi. Adem dipakai harian.' },
  { image: '/images/placeholder/sample-section3a2%201.png', title: 'Kemeja Kerja', desc: 'American drill & oxford. Potongan rapi untuk front office sampai meeting klien.' },
  { image: '/images/placeholder/sample-section3a5%201.png', title: 'Wearpack', desc: 'Drill, ripstop, atau canvas. Reflective tape opsional sesuai standar safety.' },
  { image: '/images/placeholder/sample-section3a3%201.png', title: 'Jaket Korporat', desc: 'Bomber & coach jacket untuk tim lapangan dan event perusahaan.' },
  { image: '/images/placeholder/sample-section3a1%201.png', title: 'T-Shirt Tim', desc: 'Cotton combed premium untuk seragam casual, gathering, atau onboarding.' },
];

const reasons = [
  { no: '01', title: 'On-brand, konsisten', desc: 'Warna, logo, dan potongan dijaga sama di seluruh tim — sekecil apa pun ordernya.' },
  { no: '02', title: 'Beres administrasinya', desc: 'Invoice resmi dan faktur pajak siap. Cocok untuk pengadaan perusahaan.' },
  { no: '03', title: 'Tepat waktu', desc: 'Deadline launching atau onboarding? Kami kerjakan supaya kelar sebelum hari-H.' },
];

const showcase = [
  { img: '/images/categories/20250412-section4-1.webp', alt: 'Tim kantor pakai seragam custom' },
  { img: '/images/placeholder/sample-section3a.webp', alt: 'Polo seragam perusahaan' },
  { img: '/images/categories/20250412-section4-2.webp', alt: 'Detail bordir logo perusahaan' },
];

const cta = {
  lead: 'Mau rapikan seragam tim?',
  label: 'Bikin seragam bisnis',
  wa: 'https://wa.me/6285161202499?text=' + encodeURIComponent('Halo Kustom Garment, saya mau bikin seragam / apparel kerja untuk tim/perusahaan.'),
};
---

<BaseLayout title={metaTitle} description={metaDescription}>
  <main class="cat">
    <!-- HERO (banner) -->
    <section class="cat-hero2">
      <div class="cat-banner-wrap reveal">
        <div class="cat-banner">
          <img src={hero.banner} alt={hero.bannerAlt} loading="eager" />
        </div>
      </div>
      <div class="cat-hero2-inner">
        <h1 class="cat-hero2-title reveal">{hero.title}</h1>
        <p class="cat-hero2-sub reveal">{hero.sub}</p>
      </div>
    </section>

    <!-- CUSTOMER STRIP -->
    <section class="cat-clients">
      <div class="cat-clients-marquee">
        <div class="cat-clients-track">
          {[...clients, ...clients].map((l) => (
            <div class="cat-client-card"><img src={`/images/clients/${l}`} alt="Klien Kustom Garment" loading="lazy" /></div>
          ))}
        </div>
      </div>
    </section>

    <!-- CATEGORY PEEK -->
    <section class="cat-peek">
      <div class="cat-wrap">
        <div class="cat-peek-grid">
          {categoryPeek.map((c) => (
            <a class:list={["cat-peek-item", { 'is-current': c.current }]} href={c.href}>
              <div class="cat-peek-frame">
                <img src={c.image} alt={c.name} loading="lazy" />
                {c.current && <span class="cat-peek-badge">Kamu di sini</span>}
              </div>
              <div class="cat-peek-row">
                <h2 class="cat-peek-name">{c.name}</h2>
                <span class="cat-peek-arrow" aria-hidden="true">→</span>
              </div>
              <p class="cat-peek-desc">{c.desc}</p>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- PRODUCTS -->
    <section class="cat-products">
      <div class="cat-wrap">
        <h2 class="cat-h2 reveal">Produk yang sering dibikin tim kerja</h2>
        <div class="cat-prod-grid">
          {products.map((p, i) => (
            <article class="cat-prod reveal" style={`transition-delay:${i * 60}ms`}>
              <div class="cat-prod-frame">
                <img src={p.image} alt={p.title} loading="lazy" />
              </div>
              <h3 class="cat-prod-title">{p.title}</h3>
              <p class="cat-prod-desc">{p.desc}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- WHY -->
    <section class="cat-why">
      <div class="cat-wrap">
        <h2 class="cat-h2 reveal">Kenapa tim kerja pilih kami</h2>
        <div class="cat-why-grid">
          {reasons.map((r, i) => (
            <div class="cat-card reveal" style={`transition-delay:${i * 70}ms`}>
              <span class="cat-card-no">{r.no}</span>
              <h3 class="cat-card-title">{r.title}</h3>
              <p class="cat-card-desc">{r.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- SHOWCASE -->
    <section class="cat-showcase">
      <div class="cat-wrap">
        <div class="cat-show-grid">
          {showcase.map((s) => (
            <div class="cat-show-frame reveal">
              <img src={s.img} alt={s.alt} loading="lazy" />
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="cat-cta-sec">
      <div class="cat-wrap">
        <p class="cat-cta-lead reveal">{cta.lead}</p>
        <a class="cat-cta reveal" href={cta.wa} target="_blank" rel="noopener noreferrer">
          {cta.label} <span aria-hidden="true">→</span>
        </a>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .cat { background: #ffffff; }
  .cat-wrap { max-width: 72rem; margin: 0 auto; padding: 0 1.5rem; }
  @media (min-width: 1024px) { .cat-wrap { padding: 0 2.5rem; } }

  /* HERO (banner) */
  .cat-hero2 { padding: 1.5rem 0 0; }
  @media (min-width: 1024px) { .cat-hero2 { padding: 2rem 0 0; } }
  .cat-banner-wrap { max-width: 90rem; margin: 0 auto; padding: 0 1rem; }
  @media (min-width: 1024px) { .cat-banner-wrap { padding: 0 1.5rem; } }
  .cat-banner { border-radius: 1.75rem; overflow: hidden; aspect-ratio: 4 / 5; background: #f4f4f4; }
  @media (min-width: 640px) { .cat-banner { aspect-ratio: 16 / 9; } }
  @media (min-width: 1024px) { .cat-banner { aspect-ratio: 24 / 8; } }
  .cat-banner img { width: 100%; height: 100%; object-fit: cover; display: block; }

  .cat-hero2-inner { max-width: 60rem; margin: 0 auto; padding: 2.5rem 1.5rem 0; text-align: center; }
  @media (min-width: 1024px) { .cat-hero2-inner { padding: 3rem 2.5rem 0; } }
  /* Type scale — Display */
  .cat-hero2-title { font-family: var(--font-sans); font-weight: 700; font-size: clamp(2rem, 4.5vw, 3.5rem); line-height: 1.05; letter-spacing: -0.03em; color: #1a1a1a; margin: 0 0 1rem; }
  /* Type scale — Lead */
  .cat-hero2-sub { font-family: var(--font-sans); font-weight: 600; font-size: clamp(1rem, 1.3vw, 1.0625rem); line-height: 1.6; color: #6b7280; max-width: 40rem; margin: 0 auto; }

  /* CUSTOMER STRIP */
  .cat-clients { padding: 3.5rem 0 1rem; overflow: hidden; }
  @media (min-width: 1024px) { .cat-clients { padding: 4.5rem 0 1.5rem; } }
  .cat-clients-marquee { overflow: hidden; -webkit-mask-image: linear-gradient(to right, transparent, #000 8%, #000 92%, transparent); mask-image: linear-gradient(to right, transparent, #000 8%, #000 92%, transparent); }
  .cat-clients-track { display: flex; gap: 1rem; width: max-content; animation: cat-clients-scroll 38s linear infinite; }
  @keyframes cat-clients-scroll { from { transform: translateX(0); } to { transform: translateX(calc(-50% - 0.5rem)); } }
  @media (prefers-reduced-motion: reduce) { .cat-clients-track { animation: none; flex-wrap: wrap; justify-content: center; } .cat-clients-marquee { -webkit-mask-image: none; mask-image: none; } }
  .cat-client-card {
    flex-shrink: 0; height: 5rem; width: 9.5rem; display: grid; place-items: center;
    background: #ffffff; border: 1px solid #ececec; border-radius: 1rem; padding: 1rem;
    transition: border-color 240ms ease, transform 240ms ease, box-shadow 240ms ease;
  }
  .cat-client-card img { max-height: 2.5rem; max-width: 100%; width: auto; object-fit: contain; filter: grayscale(1); opacity: 0.6; transition: filter 240ms ease, opacity 240ms ease; }
  @media (hover: hover) {
    .cat-client-card:hover { border-color: #ff4c00; transform: translateY(-3px); box-shadow: 0 10px 24px rgba(255,76,0,0.1); }
    .cat-client-card:hover img { filter: none; opacity: 1; }
  }

  /* CATEGORY PEEK */
  .cat-peek { padding: 3.5rem 0 1rem; }
  @media (min-width: 1024px) { .cat-peek { padding: 5rem 0 1rem; } }
  .cat-peek-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; }
  @media (min-width: 900px) { .cat-peek-grid { grid-template-columns: repeat(4, 1fr); gap: 1.5rem; } }
  .cat-peek-item { display: block; text-decoration: none; }
  .cat-peek-frame { position: relative; border-radius: 1.25rem; overflow: hidden; aspect-ratio: 4 / 5; background: #f4f4f4; margin-bottom: 0.9rem; }
  .cat-peek-frame img { width: 100%; height: 100%; object-fit: cover; display: block; transition: transform 800ms cubic-bezier(0.19,1,0.22,1); }
  .cat-peek-item.is-current .cat-peek-frame { outline: 2px solid #ff4c00; outline-offset: 2px; }
  .cat-peek-badge { position: absolute; top: 0.7rem; left: 0.7rem; font-family: var(--font-sans); font-weight: 700; font-size: 0.625rem; letter-spacing: 0.08em; text-transform: uppercase; color: #fff; background: #ff4c00; border-radius: 999px; padding: 0.28rem 0.6rem; }
  .cat-peek-row { display: flex; align-items: baseline; justify-content: space-between; gap: 0.5rem; }
  .cat-peek-name { font-family: var(--font-sans); font-weight: 700; font-size: 1.125rem; letter-spacing: -0.01em; color: #1a1a1a; margin: 0; transition: color 220ms ease; }
  .cat-peek-arrow { font-family: var(--font-sans); font-weight: 700; color: #ff4c00; opacity: 0; transform: translateX(-6px); transition: opacity 260ms ease, transform 260ms cubic-bezier(0.19,1,0.22,1); }
  .cat-peek-desc { font-family: var(--font-sans); font-weight: 600; font-size: 0.8125rem; line-height: 1.45; color: #6b7280; margin: 0.25rem 0 0; }
  @media (hover: hover) {
    .cat-peek-item:hover .cat-peek-frame img { transform: scale(1.06); }
    .cat-peek-item:hover .cat-peek-name { color: #ff4c00; }
    .cat-peek-item:hover .cat-peek-arrow { opacity: 1; transform: translateX(0); }
  }

  /* Shared H2 */
  .cat-h2 {
    font-family: var(--font-sans); font-weight: 700;
    font-size: clamp(1.75rem, 4vw, 3rem); letter-spacing: -0.02em;
    color: #1a1a1a; margin: 0 0 2.5rem;
  }

  /* PRODUCTS */
  .cat-products { padding: 5rem 0 1rem; }
  @media (min-width: 1024px) { .cat-products { padding: 7rem 0 1.5rem; } }
  .cat-prod-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem 1.25rem; }
  @media (min-width: 768px) { .cat-prod-grid { grid-template-columns: repeat(3, 1fr); gap: 2.25rem 1.75rem; } }
  .cat-prod { display: block; }
  .cat-prod-frame {
    position: relative; border-radius: 1.25rem; overflow: hidden;
    aspect-ratio: 4 / 3; background: #f5f5f5; display: grid; place-items: center;
  }
  .cat-prod-frame img { width: 100%; height: 100%; object-fit: contain; display: block; transition: transform 700ms cubic-bezier(0.19,1,0.22,1); }
  @media (hover: hover) { .cat-prod:hover .cat-prod-frame img { transform: scale(1.06); } }
  .cat-prod-title { font-family: var(--font-sans); font-weight: 700; font-size: 1.125rem; letter-spacing: -0.01em; color: #1a1a1a; margin: 1rem 0 0.35rem; }
  .cat-prod-desc { font-family: var(--font-sans); font-weight: 600; font-size: 0.875rem; line-height: 1.5; color: #6b7280; margin: 0; }

  /* WHY */
  .cat-why { padding: 5rem 0; }
  @media (min-width: 1024px) { .cat-why { padding: 7rem 0; } }
  .cat-why-grid { display: grid; grid-template-columns: 1fr; gap: 1.25rem; }
  @media (min-width: 768px) { .cat-why-grid { grid-template-columns: repeat(3, 1fr); gap: 1.5rem; } }
  .cat-card { background: #f5f5f5; border-radius: 1.25rem; padding: 2rem; transition: background 280ms ease, transform 280ms ease, opacity 700ms ease; }
  .cat-card-no { font-family: var(--font-sans); font-weight: 700; font-size: 0.875rem; color: #ff4c00; letter-spacing: 0.1em; }
  .cat-card-title { font-family: var(--font-sans); font-weight: 700; font-size: 1.375rem; letter-spacing: -0.01em; color: #1a1a1a; margin: 0.75rem 0 0.5rem; }
  .cat-card-desc { font-family: var(--font-sans); font-weight: 600; font-size: 0.9375rem; line-height: 1.55; color: #6b7280; margin: 0; }
  @media (hover: hover) {
    .cat-card:hover { background: #ff4c00; transform: translateY(-4px); }
    .cat-card:hover .cat-card-no { color: #ffffff; }
    .cat-card:hover .cat-card-title { color: #ffffff; }
    .cat-card:hover .cat-card-desc { color: rgba(255,255,255,0.9); }
  }

  /* SHOWCASE */
  .cat-showcase { padding: 0 0 5rem; }
  @media (min-width: 1024px) { .cat-showcase { padding: 0 0 7rem; } }
  .cat-show-grid { display: grid; grid-template-columns: 1fr; gap: 1.25rem; }
  @media (min-width: 768px) { .cat-show-grid { grid-template-columns: repeat(3, 1fr); gap: 1.5rem; } }
  .cat-show-frame { border-radius: 1.25rem; overflow: hidden; aspect-ratio: 4 / 5; background: #f4f4f4; }
  .cat-show-frame img { width: 100%; height: 100%; object-fit: cover; display: block; transition: transform 800ms cubic-bezier(0.19,1,0.22,1); }
  @media (hover: hover) { .cat-show-frame:hover img { transform: scale(1.05); } }

  /* CTA */
  .cat-cta-sec { padding: 0 0 6rem; }
  @media (min-width: 1024px) { .cat-cta-sec { padding: 0 0 8rem; } }
  .cat-cta-lead { font-family: var(--font-sans); font-weight: 600; font-size: 1rem; color: #6b7280; margin: 0 0 0.5rem; }
  .cat-cta {
    display: inline-flex; align-items: baseline; gap: 0.5rem; position: relative;
    font-family: var(--font-sans); font-weight: 700; font-size: clamp(1.75rem, 4vw, 3rem);
    letter-spacing: -0.02em; color: #1a1a1a; text-decoration: none; padding-bottom: 0.12em;
  }
  .cat-cta span { color: #ff4c00; display: inline-block; transition: transform 320ms cubic-bezier(0.19,1,0.22,1); }
  .cat-cta::after {
    content: ''; position: absolute; left: 0; bottom: 0; width: 100%; height: 3px;
    background: #ff4c00; transform: scaleX(0); transform-origin: left;
    transition: transform 460ms cubic-bezier(0.19,1,0.22,1);
  }
  @media (hover: hover) {
    .cat-cta:hover::after { transform: scaleX(1); }
    .cat-cta:hover span { transform: translateX(8px); }
  }

  /* Reveal */
  .reveal { opacity: 0; transform: translateY(24px); transition: opacity 700ms ease, transform 700ms cubic-bezier(0.19,1,0.22,1); }
  .reveal.in { opacity: 1; transform: none; }
  @media (prefers-reduced-motion: reduce) {
    .reveal { opacity: 1; transform: none; transition: none; }
    .cat-prod-frame img, .cat-show-frame img { transition: none; }
  }
</style>

<script>
  const io = new IntersectionObserver((entries) => {
    entries.forEach((e) => { if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); } });
  }, { threshold: 0.15 });
  document.querySelectorAll('.reveal').forEach((el) => io.observe(el));
</script>
