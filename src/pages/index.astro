---
/**
 * Homepage - Kustom Garment (redesign)
 */

import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/sections/Hero.astro';
import Ticker from '../components/sections/Ticker.astro';
import CategoryCards from '../components/sections/CategoryCards.astro';
import CategoryShowcase from '../components/sections/CategoryShowcase.astro';
import SceneGallery from '../components/sections/SceneGallery.astro';
import SocialProof from '../components/sections/SocialProof.astro';
import EndCta from '../components/sections/EndCta.astro';
import { categories } from '../data/categories';

const metaTitle = 'Konveksi Custom Satuan Surabaya | Kustom Garment – Desain Bebas, Tanpa Minimal Order';
const metaDescription = 'Kustom Garment adalah konveksi custom satuan di Surabaya yang melayani pembuatan kaos, jaket, kemeja, dan seragam tanpa minimal order. Desain bebas, bahan premium, dan layanan cepat.';
const metaImage = '/images/hero/banner-kustomgarment.png';
---

<BaseLayout title={metaTitle} description={metaDescription} image={metaImage}>
  <main>
    <!-- Ticker (LED-style perks) -->
    <Ticker />

    <!-- Hero (IG-story banner slider) -->
    <Hero />

    <!-- Category cards -->
    <CategoryCards />

    <!-- Category showcases (alternating layout) -->
    <CategoryShowcase category={categories.business}  reverse={false} />
    <CategoryShowcase category={categories.community} reverse={true} />
    <CategoryShowcase category={categories.campus}    reverse={false} />
    <CategoryShowcase category={categories.personal}  reverse={true} />

    <!-- Scene gallery (use-cases) -->
    <SceneGallery />

    <!-- Social wall (@kustomgarment reels) -->
    <SocialProof />

    <!-- End CTA banner -->
    <EndCta />
  </main>
</BaseLayout>
