/**
 * Catalog source — re-exports the committed catalog.json.
 * Pages import { catalog } (or default) from here.
 */
import catalog from './catalog.json';

export { catalog };
export default catalog;
